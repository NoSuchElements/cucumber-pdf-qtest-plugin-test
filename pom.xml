<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    
    <groupId>com.nosuchelements.test</groupId>
    <artifactId>cucumber-pdf-qtest-plugin-test</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>
    
    <name>Cucumber PDF QTest Plugin - Test Implementation</name>
    <description>Implementation test project demonstrating cucumber-pdf-qtest-plugin usage with per-feature PDF generation</description>
    
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <maven.compiler.source>11</maven.compiler.source>
        <maven.compiler.target>11</maven.compiler.target>
        <cucumber.version>7.7.0</cucumber.version>
        <testng.version>7.5</testng.version>
    </properties>
    
    <dependencies>
        <!-- Cucumber Dependencies -->
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-java</artifactId>
            <version>${cucumber.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-testng</artifactId>
            <version>${cucumber.version}</version>
            <scope>test</scope>
        </dependency>
        
        <!-- TestNG -->
        <dependency>
            <groupId>org.testng</groupId>
            <artifactId>testng</artifactId>
            <version>${testng.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
    
    <build>
        <plugins>
            <!-- Compiler Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>${maven.compiler.source}</source>
                    <target>${maven.compiler.target}</target>
                </configuration>
            </plugin>
            
            <!-- Failsafe Plugin for Integration Tests -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>3.0.0-M5</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            
            <!-- Cucumber PDF QTest Plugin -->
            <plugin>
                <groupId>com.nosuchelements</groupId>
                <artifactId>cucumber-pdf-qtest-plugin</artifactId>
                <version>1.0.0</version>
                <executions>
                    <execution>
                        <id>generate-pdf-reports</id>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>pdfreportperfeature</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <cucumberJsonReportDirectory>${project.build.directory}</cucumberJsonReportDirectory>
                    <cucumberPdfReportDirectory>${project.build.directory}/pdf-reports</cucumberPdfReportDirectory>
                    <qtestTagPrefix>@QTEST_TC_</qtestTagPrefix>
                    <title>QTest Integration Report</title>
                    <displayExpanded>true</displayExpanded>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>